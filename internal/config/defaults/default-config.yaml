# Bestow Configurations

# Config File Version
version: 0.1.0

# Bestow Profiles. Different profiles can have different source and destinations
profiles:
  default:
    # Source Directory (your dotfiles repo)
    source: {{ .Source }}
    # Destination Directory (your $HOME directory)
    destination: {{ .Destination }}
